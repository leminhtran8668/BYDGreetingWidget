<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/floating_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="4dp"
    android:background="@drawable/floating_bg">

    <!-- Tay cầm để kéo di chuyển, giữ nhấn để tắt panel -->
    <TextView
        android:id="@+id/drag_handle"
        android:layout_width="14dp"
        android:layout_height="30dp"
        android:text="⋮"
        android:textSize="12sp"
        android:textColor="#CCCCCC"
        android:gravity="center"
        android:layout_marginEnd="3dp" />

    <!-- Chấm xanh: Bắt đầu -->
    <View
        android:id="@+id/float_btn_start"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/btn_start_dot"
        android:layout_marginEnd="3dp"
        android:contentDescription="Bắt đầu" />

    <!-- Chấm đỏ: Kết thúc -->
    <View
        android:id="@+id/float_btn_end"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/btn_end_dot"
        android:contentDescription="Kết thúc" />

</LinearLayout>
